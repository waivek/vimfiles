240728 Rewrite all `.txt` and `.db` paths to data/`.txt` and `data/`.db` respectively
240728 Store the `vim -es  --clean --cmd "source sqlite-functions.vim" --cmd "quit!"` in some type of `edit-plugin-test.bash` file
240728 Separate the tests in sqlite-functions.vim into a separate file
# vim:ft=todo
